# Your original dictionary
original_dict = {
    "Name": "Alice",
    "Age": 30,
    "City": "New York"
}

# Convert all keys and values to lowercase using dict constructor
lowercase_dict = dict((key.lower(), str(value).lower()) for key, value in original_dict.items())

print(lowercase_dict)
