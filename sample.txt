import pandas as pd

# Assuming 'df' is your DataFrame
# Replace it with the actual variable name of your DataFrame

# Create a string for custom CSS styling
custom_css = """
<style>
    th, td {
        border: 1px solid black;
        border-collapse: collapse;
        padding: 8px;
        text-align: center;
    }
    th {
        background-color: #e6e6e6;
    }
</style>
"""

# Add the heading
heading = "<h1>Report for abc</h1>"

# Export DataFrame to HTML with custom CSS and heading
html_output = custom_css + heading + df.to_html(index=False, escape=False)

# Save the HTML to a file (optional)
with open("styled_report.html", "w") as f:
    f.write(html_output)
