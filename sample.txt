-- 1. Clean up previous tables if they exist
DROP TABLE IF EXISTS employees;
DROP TABLE IF EXISTS departments;

-- 2. Create the Departments table
CREATE TABLE departments (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(50)
);

-- 3. Create the Employees table
CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    dept_id INT -- Note: No Foreign Key constraint added intentionally for flexibility in learning joins
);

-- 4. Insert Data into Departments
-- Notice: 'Marketing' and 'Operations' have no employees assigned to them yet.
INSERT INTO departments (dept_id, dept_name) VALUES
(101, 'Engineering'),
(102, 'Human Resources'),
(103, 'Sales'),
(104, 'Marketing'),
(105, 'Operations');

-- 5. Insert Data into Employees (10 Rows)
-- Notice: 'Diana' and 'Jack' have NULL as their dept_id (they are not assigned a department).
INSERT INTO employees (emp_id, emp_name, dept_id) VALUES
(1, 'Alice', 101),
(2, 'Bob', 102),
(3, 'Charlie', 101),
(4, 'Diana', NULL),
(5, 'Evan', 103),
(6, 'Frank', 101),
(7, 'Grace', 102),
(8, 'Heidi', 103),
(9, 'Ivan', 101),
(10, 'Jack', NULL);




select * from departments;
